namespace TitanOrbit.Data
{
    /// <summary>
    /// Card rarity for shop weights and UI tinting. Serialized as int 1–5 (matches legacy assets).
    /// </summary>
    public enum CardRarity
    {
        Common = 1,
        Uncommon = 2,
        Rare = 3,
        Epic = 4,
        Legendary = 5
    }
}
